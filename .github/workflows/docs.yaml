name: Documentation

on:
  push:
    branches:
      - master
    tags:
      - v*
  pull-request:
    branches:
      - master

jobs:
  publish:
    name: Publish docs
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v5